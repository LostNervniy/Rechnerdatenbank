import { InstalledSoftware } from './installedSoftware';

describe('InstalledSoftware', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new InstalledSoftware()).toBeTruthy();
  });
});
