export class Ram {
  constructor(
    public ram_id: number,
    public name: string,
    public producer: string,
    public standard: string,
    public frequency: number,
    public capacity: number
  ) { }
}
